---
version: 2
plan:
  project-key: SER
  name: MessageQueue
  key: MESS

stages:
  - Test Stage:
      jobs:
        - Run Smoke Tests

Run Smoke Tests:
  tasks:
    - script:
        - make test-smoke

Run Exploit Tests:
  tasks:
    - script:
        - make test-exploit
