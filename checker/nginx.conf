server {
    resolver 127.0.0.1 ipv6=off;
    location / {
        try_files $uri @wsgi;
    }

    location @wsgi {
        set $target checker;
        include uwsgi_params;
        uwsgi_pass $target:3031;
    }
}